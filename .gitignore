# Build artifacts
/target
*.db
*.db-shm
*.db-wal

# Built dashboard assets — produced in CI from the dashboard repo, never committed
/static

# Env
.env
.env.local

# sqlx offline cache is committed intentionally (.sqlx/) for CI
